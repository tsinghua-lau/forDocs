name: Deploy

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: yarn
      - run: yarn install

      - name: Build
        run: yarn docs:build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: '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'
          publish_dir: docs/.vitepress/dist
